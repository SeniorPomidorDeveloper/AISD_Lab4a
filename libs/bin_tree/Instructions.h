#ifndef INSTRUCTIONS_H
#define INSTRUCTIONS_H

typedef enum Side
{
    LEFT,
    RIGHT
} Side;

typedef enum Find_status
{
    FOUND=6,
    NOT_FOUND
} Find_status;

#endif // INSTRUCTIONS_H